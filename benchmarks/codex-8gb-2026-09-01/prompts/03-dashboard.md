Turn the dashboard into a production-style telemetry explorer over the generated aggregate artifacts. Add summary cards, an SVG latency/throughput chart, service and region filters, a virtualized incident table, responsive styling, loading/error states, and accessible controls without adding a large UI framework. Add Playwright coverage for the main filtering flow and an empty/error state. Run the development server for browser verification, execute the Playwright tests, then run a production build and fix every issue found. Reuse existing processes where sensible and work only inside this repository. Complete the work efficiently, using independent workstreams only when they are genuinely useful.
