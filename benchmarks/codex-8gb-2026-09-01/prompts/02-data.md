Scale the project to the full benchmark workload in BENCHMARK_SPEC.md. Generate exactly 1,000,000 deterministic events across 16 NDJSON shards under data/generated, implement robust streaming ingestion and worker-thread aggregation, and persist compact dashboard aggregates. Add integration tests covering multi-shard processing, malformed records, deterministic output, and agreement between single-worker and multi-worker results. Run the full data generation, integration tests, and the analytics benchmark. Fix failures and leave the generated benchmark artifacts available for the next task. Work only inside this repository. Complete the work efficiently, using independent workstreams only when they are genuinely useful.
