Implement the first complete vertical slice described in BENCHMARK_SPEC.md. Create the shared event schemas, deterministic NDJSON generator, streaming analytics package with worker-thread support, and a minimal Next.js dashboard that renders aggregate data. Add meaningful unit tests for generation, validation, and aggregation. Keep root commands working and document the architecture briefly. Run type checking and the smallest relevant test set, fixing all failures you encounter. Work only inside this repository. Complete the work efficiently, using independent workstreams only when they are genuinely useful.
